Place future `.mp4` case-study files in this folder.

Current data entries in `src/data/site/case-studies.ts` already point to paths like:

- `/videos/cases/brand-alpha-teaser.mp4`
- `/videos/cases/masterclass-x-opening.mp4`
- `/videos/cases/cannabia-brand-film.mp4`

The case details page already supports `type: "video"` entries via poster/preview layout, so
real video files can be dropped here later without changing the component structure.
